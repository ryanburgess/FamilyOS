
import Foundation
import Combine

struct GoogleCalendarService {
    func createEvent(_ event: FamilyEvent) async throws {
        // Future: route through Google Calendar API, likely via a tiny local backend/OAuth helper.
    }
}
